<%= render 'shared/header', header: 'Timeline' %>

<ul>
  <% @statuses.each do |status| %>
    <li><%= status.body %> created by <%= status.creator.username %> at <%= status.created_at%></li>
  <% end %>
</ul>
